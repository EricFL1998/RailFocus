# Rail Focus 真实同车旅客（真实在线·路段匹配）设计方案

## 1. 核心定位与原则

### 一、 核心原则：100% 纯真实，零虚假数据
- **绝不使用算法伪造或模拟虚假人数**：没有其他用户就真实展示 0（或仅提示自身），有则真实展示；
- **同路段动态交集匹配**：不仅是起终点完全一致才算同车；只要其他用户的旅程在**同一物理路段/同一车站区间**发生时空重叠，即视为“同车次旅客”；
- **途径站真实上下车感知**：列车每驶入/驶出一个车站，根据真实其他用户的开始与结束，提示“XX站有 X 位旅客上车/下车”；
- **终点站真实纪念面板**：旅程结束后，展示在本次专注全程中，与你曾经“同乘并肩专注”过的真实旅客总数及各区间相遇记录；
- **零成本方案（0 元运营）**：利用永久免费的 Serverless 基础设施（如 Supabase 免费层），无需购买任何云服务器。

---

## 2. 真实路段匹配与上下车判定逻辑

### 一、 什么是“同车乘客”（区间交集算法）
高铁上旅客的起终点各不相同，但在同一个物理区间内共处一列车。
例如：
- 用户 A：**北京南 → 上海虹桥**（途经济南西、南京南）
- 用户 B：**济南西 → 南京南**

当列车运行到 **济南西 → 南京南** 这个区间且两人同时处于专注状态时，用户 A 和 用户 B 即互为**同车乘客**。

```
时间轴与路线覆盖：
用户 A: [北京南] ───────> [济南西] ═══════════════> [南京南] ───────> [上海虹桥]
用户 B:                    [济南西] ═══════════════> [南京南]
                                  ▲               ▲
                                  │               │
                            用户B在济南西上车   用户B在南京南下车
                          （A提示: 济南西 1人上车）（A提示: 南京南 1人下车）
```

---

## 3. 零成本技术架构设计（Supabase 永久免费层）

Supabase 提供了极其强大的 PostgreSQL + 实时订阅能力，其**永久免费配额（Free Tier）**包含：
- **0 元费用**：不需要绑定信用卡即可创建；
- **500,000 次/月** API 请求（对于小众专注 App 绰绰有余）；
- **200 个并发实时长连接**（或采用轮询心跳方式，容量可轻松支撑数万日活）；
- **500 MB 数据库存储**（仅存临时心跳记录，单表占用极小）。

### 一、 极简数据表设计（单表完成全部匹配）

```sql
-- 活跃专注旅客心跳表（专注结束或超时自动视为下车）
CREATE TABLE active_journeys (
    id UUID PRIMARY KEY,                   -- 旅程唯一 ID
    user_identifier TEXT NOT NULL,         -- 匿名化设备指纹/随机用户 ID
    seat_info TEXT NOT NULL,               -- 分配的座位（如 05车08A）
    start_station_name TEXT NOT NULL,      -- 该旅客旅程的始发站（如 "北京南"）
    end_station_name TEXT NOT NULL,        -- 该旅客旅程的终点站（如 "上海虹桥"）
    focus_type TEXT NOT NULL DEFAULT '通用',-- 专注类别（如 "深度工作"、"考研自习"、"阅读"）
    station_ids TEXT[] NOT NULL,           -- 本次旅程规划经过的所有站点 ID 数组
    current_segment_from TEXT NOT NULL,    -- 当前所在区间的出发站 ID
    current_segment_to TEXT NOT NULL,      -- 当前所在区间的下一站 ID
    current_segment_index INT NOT NULL,    -- 当前在第几段区间
    status TEXT NOT NULL DEFAULT 'RUNNING',-- RUNNING / PAUSED / COMPLETED
    started_at TIMESTAMPTZ NOT NULL,       -- 开始时间
    last_heartbeat TIMESTAMPTZ NOT NULL DEFAULT NOW() -- 心跳保活时间
);

-- 为当前区间建立索引，实现毫秒级路段交集查询
CREATE INDEX idx_active_segment ON active_journeys (current_segment_from, current_segment_to);
```

### 二、 客户端心跳与状态同步（极低流量）
1. **发车（进入专注）**：
   - 上报 `active_journeys`，写入自身路线站点数组及起点区间。
2. **行驶中（轻量保活心跳）**：
   - 每 45 秒或每次列车到达下一区段时，更新一次心跳与当前区间 (`current_segment_from`, `current_segment_to`)；
   - 查询当前正在同一区间专注的真实人数：
     ```sql
     SELECT id, seat_info, start_station_name, end_station_name, focus_type, started_at
     FROM active_journeys 
     WHERE current_segment_from = :currentFrom 
       AND current_segment_to = :currentTo
       AND id != :myJourneyId
       AND last_heartbeat > NOW() - INTERVAL '90 seconds';
     ```
3. **靠站/到站（触发上下车事件）**：
   - 到达新车站时，比对上一区间的同乘列表与当前区间的同乘列表；
   - 新增的乘客触发：**“【XX站】有 1 位旅客上车”**；
   - 离开的乘客触发：**“【XX站】有 1 位旅客下车”**。
4. **终点到站 / 取消**：
   - 旅程状态标记为 `COMPLETED` 并退出心跳；
   - 超过 90 秒无心跳的异常中断设备自动被服务端过滤，保证人数绝不虚标。

---

## 4. UI 界面与交互设计

### 一、 专注进行中：车厢实时客流屏与同乘旅客列表

1. **常驻微型状态标**
   - **位置**：专注界面右上角或控制区旁的微型胶囊标。
   - **状态展示**：
     - **当前无人同乘**：展示 `[ 💺 独自前行 ]`；
     - **有真实同乘**：展示 `[ 💺 同车旅客 2 人 ]`。

2. **【同车旅客列表】极简抽屉（点击展开）**
   - 点击状态标自底部滑出极度纯净的同乘旅客卡片列表。
   - **严格遵循极简与隐私原则：仅展示三项核心信息，其余一概不展示**：
     - **始发站 ➔ 终点站**：例如 `北京南 ➔ 上海虹桥`
     - **专注类别**：例如 `深度工作`、`背单词`、`考研自习`
   - **卡片原型示意**：
     ┌──────────────────────────────────────────────┐
     │  北京南 ➔ 上海虹桥            [ 深度工作 ]    │
     └──────────────────────────────────────────────┘
   - **零多余元素**：不展示用户名、不展示头像、不展示座位号、不展示在途时长，杜绝任何窥探感与社交压力。

### 二、 到站时动态：真实上下车微播报
- 当列车行驶到某一个中间车站时，若检测到真实用户变动，在车站信息卡上方弹出轻量文字吐司（Toast/Banner）：
  - 🟢 *“列车到达徐州东站，有 1 位旅客上车与你同行”*
  - ⚪ *“列车离开蚌埠南站，同乘旅客在蚌埠南站下车”*

### 三、 旅程结束：真实同乘纪念面板
- 倒计时结束、到达终点站后，在 `CompletionOverlay` 完成面板中增加极其克制简约的**【同乘纪念】**区域：
- **严格遵循极简原则：仅显示同车人数，不展示任何冗余列表或相遇流水**：
  - 若全程独自一人：展示 *“同车旅客：独自包车”* 或 *“同乘旅客：0 人”*；
  - 若有真实同行者：展示 *“同乘旅客：X 人”*（如：*“本次旅程共有 3 位真实旅客与你同车并肩专注”*）。

---


---

---

## 5. 国内直连可用后端选型（完全免费额度 / 月调用几百至几千次）

针对 LeanCloud 停止个人免费版服务的现状，以及初期每月仅几百至几千次轻量调用的特征，精选出当前在**中国大陆网络直连稳定、拥有长期免费额度（真正0元起跑）、且支持超低成本平滑扩容**的四家优质服务商：

| 平台名称 | 免费额度（每月千次调用完全 0 元） | 国内网络直连表现 | 接入与维护难易度 | 综合评价与建议 |
| :--- | :--- | :--- | :--- | :--- |
| **MemFire Cloud (国内版 Supabase)** | **免费赠送 1 个云端 PostgreSQL 数据库**<br>每月赠送数十万次 API 请求，无休眠 | **部署在阿里云国内多线 BGP 机房，三网直连延迟 15~30ms** | 兼容标准 Supabase 官方 Kotlin SDK 与 PostgREST 协议，开箱即用 | **★★★ 强烈推荐首选（纯正国内节点，完全不用翻墙，免费额度极充裕）** |
| **Bmob 后端云 (国内独立开发老牌 BaaS)** | **每天免费 10,000 次 API 请求**<br>（相当于每月 30 万次，完全覆盖初期需求） | **国内专线机房直连，无需代理** | 提供官方 Android 原生 SDK，直接操作对象，两行代码完成查询与上报 | **★★★ 快速开发首选（类似原 LeanCloud 体验，免写后端）** |
| **微信云开发 / 腾讯云 CloudBase (按量/免费体验)** | 新用户提供免费体验包，后续按量微扣费（每月几千次调用约 **0.5 ~ 2 元**） | **腾讯微信生态专属机房，三网极速** | 官方提供 HTTP API 与轻量云函数，零服务器运维 | **★★☆ 大厂兜底备选** |
| **Laf 云开发 (开源 Serverless 大陆直连)** | 免费提供基础开发容器与 MongoDB，支持随时导出迁移 | **国内节点直连极速** | Web 端几行 JS 即可编写云函数，适合极客开发者 | **★★☆ 备选方案** |

---

### 推荐落地对比与选型建议

#### 🏆 最优推荐 1：MemFire Cloud（国内版 Supabase，推荐指数 ★★★★★）
- **为什么最合适**：
  - 本质是部署在**阿里云国内节点**的 Supabase，完全不受国外网络墙限制，也不需要科学上网；
  - 拥有永久免费项目额度，且免费层数据库**不会强制冷启动休眠**；
  - 官方支持完整的 Kotlin/Java 客户端库，用 SQL 或 ORM 过滤当前路段的同车旅客极其简单：
    ```sql
    -- 查询当前路段且90秒内活跃的真实旅客（仅带回始发站、终点站、专注类别）
    SELECT start_station_name, end_station_name, focus_type
    FROM active_journeys
    WHERE current_segment_from = :from 
      AND current_segment_to = :to
      AND last_heartbeat > NOW() - INTERVAL '90 seconds';
    ```

#### 🏆 最优推荐 2：Bmob 后端云（国内老牌 Android BaaS，推荐指数 ★★★★☆）
- **为什么最合适**：
  - 开发体验与原 LeanCloud 几乎一致，在 Android Studio 中依赖其 SDK，直接面向对象调用：`BmobQuery<ActiveJourney>`；
  - 每天 10,000 次免费请求，对于你初期一个月几百到几千次调用，使用量占比连 1% 都不到，永久安全白嫖。

## 6. 实施路线规划

| 模块 | 核心工作内容 | 成本 |
| :--- | :--- | :--- |
| **云端基础设施搭建** | 创建免费 Supabase 项目，配置 `active_journeys` 心跳表与基于区间的查询函数 | **0 元** |
| **Android 客户端心跳 SDK** | 在 `FocusTimerService` 中增加心跳调度器，每 45s 更新区间与拉取同车人数 | **0 元** |
| **区间交集比对逻辑** | 本地比对前后区间乘客变化，派发“上车 / 下车”事件 | **0 元** |
| **UI 与到站纪念面板** | 专注页同车人数电子屏、上下车弱提示浮窗、终点站真实同乘纪念卡 | **0 元** |

